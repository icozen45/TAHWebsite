"use client";

import { useRef, useState } from "react";
import {
  Globe,
  Handshake,
  Trophy,
  ShieldCheck,
  MessageSquare,
  RotateCcw,
  Lock,
  ScanLine
} from "lucide-react";
import { motion, useInView } from "framer-motion";
import Link from "next/link";
import CountUp from "react-countup";
import { howItWorks } from '@/data/homepageSections';
import { extraProcessDetails } from "@/data/extraProcessDetails";
import Drawer from "@/app/components/drawer"; // Your custom Drawer

export default function Home() {
  const statsRef = useRef(null);
  const isInView = useInView(statsRef, { once: true });
  const [showDrawer, setShowDrawer] = useState(false);

  const stats = [
    {
      title: "Projects Completed",
      value: 14000,
      suffix: "+",
      icon: Globe,
    },
    {
      title: "Years of Experience",
      value: 12,
      suffix: "+",
      icon: Trophy,
    },
    {
      title: "Success Rate",
      value: 98.9,
      suffix: "%",
      icon: Handshake,
    },
  ];

  return (
    <main className="font-sans text-gray-800 bg-white">
      {/* HERO */}
      <section className="relative w-full h-[100dvh] overflow-hidden flex items-center justify-center">
        <video
          autoPlay
          muted
          loop
          playsInline
          className="absolute inset-0 w-full h-full object-cover pointer-events-none"
          src="background-anim.mp4"
        />
        <div className="absolute inset-0 bg-black/50 backdrop-blur-sm z-10" />
        <div className="relative z-20 text-center px-4 max-w-2xl mx-auto">
          <h1 className="text-4xl sm:text-5xl md:text-5xl font-bold text-white leading-tight">
            Global Project Solutions
          </h1>
          <p className="mt-6 text-lg sm:text-xl text-white/90">
            Discover trusted services and reviews — fast, simple, and reliable.
          </p>
          <Link
            href="/services"
            className="inline-block mt-8 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-full shadow-lg transition"
          >
            Get Started
          </Link>
        </div>
      </section>

      {/* TRUST BANNER */}
      <section className="bg-blue-50 py-16 px-4 text-center">
        <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 max-w-4xl mx-auto">
          Trusted by professionals around the globe.
        </h2>
        <p className="mt-6 text-base sm:text-lg text-gray-600 max-w-2xl mx-auto">
          Global Project Solutions empowers thousands to discover, evaluate,
          and connect with real-world services.
        </p>
      </section>

      {/* ABOUT */}
      <section className="bg-white py-16 px-4 max-w-6xl mx-auto">
        <div className="flex flex-col md:flex-row items-center gap-10">
          <div
            className="w-full md:w-1/2 h-64 bg-gray-100 rounded-lg shadow-inner bg-cover bg-center border border-gray-200 transition-transform duration-300 hover:scale-101"
            style={{ backgroundImage: "url('/writing_template.png')" }}
          />
          <div className="w-full md:w-1/2 text-center md:text-right">
            <h2 className="text-3xl md:text-4xl font-bold mb-4 text-gray-900">
              About Us
            </h2>
            <p className="text-base sm:text-lg text-gray-700 leading-relaxed">
              At Global Project Solutions, we believe in bridging gaps through
              intelligent platforms that empower users. Whether you're a startup
              founder, a research scholar, or a corporate professional — our
              tools are designed to help you discover, connect, and grow with
              confidence. Our vision is to create a world where quality services
              are just a click away.
            </p>
          </div>
        </div>
      </section>

      {/* WHY CHOOSE US */}
      <section className="bg-blue-50 py-20 px-4">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-3xl md:text-4xl font-bold text-center mb-12 text-gray-900">
            Why Choose Us?
          </h2>

          <div className="flex flex-col md:flex-row gap-8">
            {[
              {
                title: "Expertise You Can Trust",
                desc:
                  "Our team brings over a decade of industry experience across a wide range of fields. From small businesses to large enterprises, we deliver tailored solutions with professionalism.",
              },
              {
                title: "Seamless Collaboration",
                desc:
                  "We believe in communication. Our clients stay informed at every step, ensuring a smooth and collaborative project experience from start to finish.",
              },
              {
                title: "Proven Track Record",
                desc:
                  "With thousands of successful projects completed, our consistent performance and client satisfaction speak volumes. We're here to make your next project a success too.",
              },
            ].map((item, i) => (
              <div
                key={i}
                className="bg-white border-l-4 border-blue-600 shadow-sm p-6 rounded-lg flex-1"
              >
                <h3 className="text-xl font-semibold text-gray-900 mb-2">
                  {item.title}
                </h3>
                <p className="text-gray-700 text-base leading-relaxed">
                  {item.desc}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* HOW IT WORKS */}
      <section className="max-w-6xl mx-auto px-4 py-20">
        <h2 className="text-3xl font-bold text-center mb-16 text-gray-900">
          How It Works
        </h2>

        <div className="flex flex-col md:flex-row gap-12 pl-3">
          {/* LEFT: Timeline Steps */}
            <div className="md:w-1/2 relative border-l-2 border-blue-400 pr-6 space-y-16 ">
              {howItWorks.map((card, i) => (
                <div key={i} className="relative pl-12">
                  <div className="absolute left-[-1.25rem] top-1/2 transform -translate-y-1/2 w-10 h-10 rounded-full bg-white border-2 border-blue-500 text-blue-600 font-bold flex items-center justify-center shadow-sm">
                    {i + 1}
                  </div>
                  <div className="bg-white border border-gray-100 rounded-lg shadow-sm hover:shadow-md transition-all p-5">
                    <div className="flex items-center gap-3 mb-1 text-blue-600">
                      {card.icon}
                      <span className="text-sm font-semibold uppercase tracking-wide">
                        {card.step}
                      </span>
                    </div>
                    <h3 className="text-lg font-semibold text-gray-800">{card.title}</h3>
                    <p className="text-sm text-gray-600 leading-relaxed mt-1">
                      {card.desc}
                    </p>
                  </div>
                </div>
              ))}
            </div>

            {/* RIGHT: Extra Info (Drawer on mobile, inline on desktop) */}
            <div className="md:w-1/2 relative flex flex-col justify-start gap-6">
              <h3 className="text-xl font-bold text-gray-800">
                What Else Happens Behind the Scenes?
              </h3>

              {/* Mobile Drawer Trigger */}
              <div className="md:hidden">
                <button
                  onClick={() => setShowDrawer(true)}
                  className="mt-2 inline-block bg-blue-600 text-white px-5 py-2 rounded-full shadow hover:bg-blue-700 transition w-fit"
                >
                  View Details
                </button>
              </div>

              {/* Desktop Inline Content - Added max-h-[330px] and overflow-y-auto */}
              <div className="hidden md:grid grid-cols-1 gap-6 max-h-[510px] overflow-y-auto pr-2">
                {extraProcessDetails.map((item, idx) => {
                  const Icon = item.icon;
                  return (
                    <div
                      key={idx}
                      className="bg-gray-50 border border-gray-100 rounded-xl p-5 shadow-sm hover:shadow-md transition"
                    >
                      <div className="flex items-start gap-4">
                        <Icon className="w-12 h-12 text-blue-500 mt-1" />
                        <div>
                          <p className="text-sm font-semibold text-gray-800 mb-1">{item.title}</p>
                          <p className="text-sm text-gray-600">{item.desc}</p>
                        </div>
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
          </div>
        </section>

      {/* DRAWER FOR EXTRA PROCESS */}
      <Drawer
        isOpen={showDrawer}
        onClose={() => setShowDrawer(false)}
        title="Behind the Scenes"
      >
        <div className="grid grid-cols-1 gap-6 max-h-[70vh] overflow-y-auto pr-2">
          {extraProcessDetails.map((item, idx) => {
            const Icon = item.icon;
            return (
              <div
                key={idx}
                className="bg-gray-50 border border-gray-100 rounded-xl p-5 shadow-sm hover:shadow-md transition"
              >
                <div className="flex items-start gap-4">
                  <Icon className="w-12 h-12 text-blue-500 mt-1" />
                  <div>
                    <p className="text-sm font-semibold text-gray-800 mb-1">{item.title}</p>
                    <p className="text-sm text-gray-600">{item.desc}</p>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </Drawer>

      {/* STATS */}
      <section className="w-full px-4 py-16 bg-white text-center" ref={statsRef}>
        <h2 className="text-3xl font-bold mb-10 text-gray-900">
          Our Impact in Numbers
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-10 max-w-5xl mx-auto">
          {stats.map((stat, index) => {
            const Icon = stat.icon;
            return (
              <div key={index} className="flex flex-col items-center">
                <Icon className="w-10 h-10 text-indigo-600 mb-2" />
                <h3 className="text-4xl font-extrabold text-gray-800">
                  {isInView ? (
                    <CountUp end={stat.value} duration={2} suffix={stat.suffix || ""} />
                  ) : (
                    0
                  )}
                </h3>
                <p className="text-gray-600 mt-1">{stat.title}</p>
              </div>
            );
          })}
        </div>
      </section>

      {/* CTA */}
      <section className="w-full bg-blue-600 py-20 px-4 text-center text-white">
        <h2 className="text-3xl md:text-4xl font-bold mb-4">
          Ready to get started?
        </h2>
        <p className="mb-8 text-lg">
          Join thousands of users already making smarter decisions.
        </p>
        <Link
          href="/services"
          className="inline-block bg-white text-blue-600 hover:bg-gray-100 px-6 py-3 rounded-full font-semibold"
        >
          Place an Order
        </Link>
      </section>
    </main>
  );
}
